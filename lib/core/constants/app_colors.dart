import 'package:flutter/material.dart';

/// Defines the core color palette for the application
class AppColors {
  // Prevent instantiation
  AppColors._();

  static const Color warmSand = Color(0xFFF5F5DC);
  static const Color terracotta = Color(0xFFE2725B);
  static const Color deepNavy = Color(0xFF0A192F);
  static const Color white = Colors.white;
}